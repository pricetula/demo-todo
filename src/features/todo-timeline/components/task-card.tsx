"use client";

// ─── Individual task card rendered inside the timeline ─────────────────────

export function TaskCard() {
  return <div className="rounded-md border p-4 shadow-sm" data-component="task-card" />;
}
