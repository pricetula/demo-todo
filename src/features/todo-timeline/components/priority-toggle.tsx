"use client";

// ─── Toggle / badge for changing a task's priority level ───────────────────

export function PriorityToggle() {
  return <div className="inline-flex items-center gap-1" data-component="priority-toggle" />;
}
